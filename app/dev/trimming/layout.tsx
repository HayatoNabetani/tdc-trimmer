import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '【検証用】トリミング見積もり',
};

export default function DevTrimmingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
